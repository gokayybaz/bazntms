# Vite `--mode demo` bu dosyayı yükler (npm run build:demo).
# Demo build'i: backend yok — window.fetch + WebSocket sentetik veri katmanına
# yönlendirilir (src/demo/). GitHub Pages'te /bazntms/demo/ altında yayınlanır.
VITE_DEMO=1
